export default defineAppConfig({
  ui: {
    // success sur le vert Footix : le vert par défaut de Nuxt UI est trop pâle sur le fond vert de l'appli.
    colors: { primary: 'footix', success: 'footix', neutral: 'neutral' },
  },
})
